<?xml version="1.0" encoding="utf-8"?>
<!-- Cleartext allowed so LAN BirdNET-Pi (e.g. http://192.168.1.159) works.
     Prefer HTTPS when the Pi is exposed publicly. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
